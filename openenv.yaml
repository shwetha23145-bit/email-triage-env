name: email-triage-env
description: AI environment for classifying emails
version: 1.0

observation_space:
  email_text: string

action_space:
  category: string
  priority: string
  action: string

reward: 0.0 to 1.0